<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BracketX"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom Scheme Deep Links (e.g. bracketx://tournament/{id}) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="bracketx"
                    android:host="tournament" />
            </intent-filter>

            <!-- Universal Web App Links (Opens app if installed on Android) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="bracketx.vercel.app"
                    android:pathPrefix="/tournament" />
                <data
                    android:scheme="https"
                    android:host="*.vercel.app"
                    android:pathPrefix="/tournament" />
                <data
                    android:scheme="http"
                    android:host="bracketx.vercel.app"
                    android:pathPrefix="/tournament" />
                <data
                    android:scheme="http"
                    android:host="*.vercel.app"
                    android:pathPrefix="/tournament" />
            </intent-filter>
        </activity>
    </application>

</manifest>
